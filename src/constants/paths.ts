import { profile } from "console";

export const paths = {
    home: "/",
    login: "/login",
    register: "/register",
    products: "/products",
    profile: "/profile",
    cart: "/cart",
    refreshToken: "/refreshToken",
};